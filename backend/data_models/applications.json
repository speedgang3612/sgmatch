{
    "title": "applications",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "user_id": {
            "type": "string",
            "description": "User ID (지원한 라이더)"
        },
        "job_listing_id": {
            "type": "integer",
            "description": "채용공고 ID"
        },
        "agency_name": {
            "type": "string",
            "description": "지사명"
        },
        "rider_name": {
            "type": "string",
            "description": "라이더 이름"
        },
        "status": {
            "type": "string",
            "description": "상태: pending, contacted, matched, closed"
        },
        "created_at": {
            "type": "string",
            "format": "date-time",
            "description": "생성일"
        }
    },
    "required": [
        "id",
        "user_id"
    ]
}